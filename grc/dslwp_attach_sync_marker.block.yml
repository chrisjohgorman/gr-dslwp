# auto-generated by grc.converter

id: dslwp_attach_sync_marker
label: Attach Sync Marker
category: '[DSLWP]'

parameters:
-   id: marker
    label: Marker
    dtype: raw
-   id: data_format
    label: Data Format
    dtype: enum
    default: '1'
    options: ['1', '2']
    option_labels: [Bits, Packed bits]

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import dslwp
    make: dslwp.attach_sync_marker(${marker}, ${data_format})

file_format: 1
